<%= form_for(@form, url: choose_subsidiary_admin_move_advisers_path, method: :get) do |f| %>
  <h1>Firm to move to</h1>

  <%= render 'admin/shared/errors', model: @form %>

  <div class="form-group">
    <%= f.label :destination_firm_fca_number, 'FCA Number of firm to move to:' %>
    <%= f.text_field :destination_firm_fca_number, class: 't-destination-firm-fca-number' %>
  </div>

  <div class="t-hidden-fields form-group">
    <% @form.adviser_ids.each do |adviser_id| %>
      <%= f.hidden_field(:adviser_ids, multiple: true, value: adviser_id, class: 't-adviser') %>
    <% end %>
  </div>

  <div class="form-group">
    <%= f.submit('Next', class: 't-next') %>
  </div>
<% end %>
