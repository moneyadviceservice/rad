<li class="dashboard-list__item t-firm-list-item <%= 't-trading-name' if firm_type == :trading_name %>">
  <div class="dashboard-list__item__type t-type">
    <%= t("dashboard.#{firm_type.to_s}") %>
  </div>
  <div class="dashboard-list__item__name t-name">
    <%= firm.registered_name %>
  </div>
</li>
