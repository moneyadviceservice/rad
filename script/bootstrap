set -e

psql -c '\du' postgres | grep -q ' postgres .*Superuser' \
    || createuser -ds postgres

npm install
npm install -g bower

rbenv install -s
gem install bundler
bundle install
bundle exec bowndler update
